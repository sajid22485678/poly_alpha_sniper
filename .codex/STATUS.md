# Poly Alpha status

Successor34 is terminal `OOS_FAILED`. Post-stop inspection found eight unhandled REST-recovery `KeyError` task failures in runtime stderr during the guarded window. Because detached task exceptions were discarded without being retrieved or latched, the guardian’s otherwise clean 468-observation completion was false-green. The earlier immutable data-insufficient review remains preserved; its 298/300 frozen result is still factual, but the binding software/monitor failure overrides its classification.

Successor34 stopped gracefully exactly once, released its lease, has zero open sessions, and reconciled 41,302/41,302 committed commands with zero failed or unresolved commands. It is permanently `MUST_NOT_RELAUNCH`; its guardian is `MUST_NOT_RESTART`.

The causal reconnect/book-buffer race and background-task monitoring gap have RED regressions and 237 affected tests passing. A Successor35-named source seal then passed exactly once: 4,258/4,258, zero failures/errors/skips, tree `9ccac82941cd0be674060c89007a32dcc028f42572371eaf486c9099ae74d643`, exact post-tree equality, authoritative v5 byte-identical. Under the current owner packet this seal is evidence only: no materialization or prospective launch is authorized until the deep stabilization campaign is exhausted. Any further source mutation supersedes it.

Exact next action: complete the cumulative predecessor failure matrix and adjacent-risk audit, then run the staged deterministic stress, crash/recovery, guardian, shutdown, and bounded-soak campaign. Do not materialize or launch Successor35.

Safety remains fail-closed: live disabled, real orders impossible, signing/authenticated trading absent, kill switch engaged, no Phase Two/Three, V4-HO-001 protected, authoritative v5 immutable.
