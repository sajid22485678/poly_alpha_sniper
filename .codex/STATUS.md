# Poly Alpha status

Successor29 is terminal and permanently ineligible. Its first binding guardian failure is `V4PersistenceTimeout` at `1787747461243`: command 11828 retained a legitimate 5,752-row CEX evidence burst and acknowledged after 60,738 ms against the 15,000 ms deadline. The immutable failure artifact is `guardian_failure_1787747461243.json`, SHA-256 `bcdeea717b989319cc458ad248736286bc838eec3dca4d6e112d231abef2a3f2`. A later `V4PersistenceIdempotencyConflict` was downstream; it does not replace or cure the first result.

The exact graceful stop authority was consumed once. Runtime `13504 -> 3256` and guardian `14908 -> 16368` are absent, the lease and request are released, and the database reconciles 12,182/12,182 committed commands with zero incomplete, failed, duplicate, retried, mismatched, or truly lost durable rows. The terminal closure is `successor29_terminal_closure.json`, SHA-256 `3633d4d5357b6b7188ca13e20fb37fc02b472203c30bcd5d2c334d7686d580d8`.

The causal defect was transaction write amplification: one source and CEX insert per retained observation plus repeated reconstruction and hashing of the same capsule graph. The minimal correction batches first-seen source/CEX and graph rows and reuses one independently validated derived graph. RED tests were observed for cursor reads, unbounded insert statements, and triple graph derivation. Focused store/replay tests and the persistence, runtime, engine, prospective, schema, research, fee-gate, bot, and guardian integration files are green.

An exact 5,752-row causal qualification completed in 1,296.637 ms, below the 15,000 ms acknowledgement deadline, with 5,752 source rows, 5,752 CEX rows, 5,752 candidate links, bounded insert statements, zero cursor reads, SQLite integrity `ok`, and zero FK violations. Result SHA-256: `2f5000eb772c4586a35826567b7a9f6296b936944f591a5c4ecd580c98f45450`.

The source now differs from the qualified S28 tree. The next action is one fresh exact source seal for a distinct Successor30. Successor29 must never be relaunched or reused. All live capabilities remain absent, the kill switch is engaged, Phase Two/Three and holdout remain untouched, V4-HO-001 is nonexistent/unconsumed, and authoritative v5 remains immutable.
